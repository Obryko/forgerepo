# GitHub workflow rules

Canonical planning rules live in `CONTRIBUTING.md` and `docs/product/roadmap-model.md`.

- Stories always have Task sub-issues.
- Hierarchy and dependency order are separate.
- Spike findings create explicit follow-up work.
- Branches use `<type>/<issue>/<slug>`.
- Completed issues use `Closes #<issue>` in PRs.
